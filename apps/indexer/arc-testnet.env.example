# Isolated Arc testnet indexer. Do NOT use these values on the Robinhood Railway service.
# Postgres may be shared only because every table is keyed by chain_id.
# fused_sync_cursor PRIMARY KEY is chain_id — Arc 5042002 cannot overwrite 46630.

CHAIN_ID=5042002
NEXT_PUBLIC_CHAIN_ID=5042002
RPC_URL=https://rpc.testnet.arc.io
FUSED_PUBLIC_NETWORK=arc-testnet
FUSED_VIRTUAL_QUOTE_WEI=50000000000000000
FUSED_VIRTUAL_TOKEN=1000000000000000000000000000
FUSED_GRADUATION_TARGET_WEI=10000000000000000
FUSED_FEE_BPS=100
FUSED_LP_FEE=10000
